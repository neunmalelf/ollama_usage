- **20260822194128**: Added a `decorator_color` constant (grey) in cli.py and used it for the `|` separator and the first `-` in --minidisplay, for the autorefresh footer descriptive text ("next refresh in ... seconds at ..."), and for the help line "Shows a timestamp footer with the next refresh time." Added the missing "grey" key to the _ANSI map. Bumped the version major part to 1 (0.2.x -> 1.2.x).
