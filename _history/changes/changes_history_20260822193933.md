- **20260822193933**: Added named time-color constants (_DAYS_COLOR/_HOURS_COLOR/_MINUTES_COLOR/_SECONDS_COLOR/_VALUE_COLOR/_LABEL_COLOR) in cli.py, gui.py and widget.py; --minidisplay now shows the plan as `olu (<plan>)` and colors the `dd hh mm` remaining time; widget countdown now uses the time colors. Fixed widget right-click menu: restored the missing _show_menu method (removed the stray finally block that had been merged into _save_position) so closing via the menu no longer errors or hangs. Widget now persists its size (compact/full) alongside position; GUI already persisted geometry. --autorefresh help now shows the timestamp-footer line on its own line.
