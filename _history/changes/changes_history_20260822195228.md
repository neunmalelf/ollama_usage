- **20260822195228**: Fixed the widget (and GUI) close error/hang. The _quit methods called sys.exit(0) inside a Tkinter callback, which raised SystemExit and produced the Tcl error "while executing ..._quit (menu invoke)" and a delayed exit. Removed sys.exit(0) so closing destroys the root and lets the mainloop exit naturally. Set the version to 1.0.x (major 1, minor 0).
