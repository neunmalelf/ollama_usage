- **20260822195355**: Fixed the version shown by --version. _get_version() preferred the installed package metadata (stale, 0.1.20280807093000) over the source __version__. It now returns the source __version__ (1.0.0). Removed the unused importlib.metadata import. The compiled dist/ollama-usage.exe still needs a rebuild to pick up the new version.
