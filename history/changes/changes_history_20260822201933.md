- **20260822201933**: The % symbol now uses the label color (white) everywhere: CLI normal display and minidisplay (_color_pct colors the number by severity and the % in the label color), the widget full view (number and % rendered separately), and the widget mini line (_mini_segments splits the % into the label color). The GUI already used the label color for %.
