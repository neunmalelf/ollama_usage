- **20260911213613**: 2.0.20260911213613Z major release. Settings moved to ~/.config/ollama_usage/ (gui.cfg, widget.cfg; created on first start; dotted legacy ~/.ollama-usage-*.cfg files migrate automatically; --reset-settings updated). GUI: window auto-sizes to its content (fixed 640x300 clipped the text), no longer resizable, no scrollbar, obsolete geometry setting removed; --theme dark/light/minimal now drives the GUI darkmode (previously widget-only). CLI/widget/GUI: single space before the % sign everywhere. --minidisplay single-shot no longer clears the terminal at the end of the program. On a light background yellow (days countdown, 50-80% percentages) darkens to readable amber. Test isolation fixed: unit tests no longer write mock values into the real state files.