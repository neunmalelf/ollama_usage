- **20260827230230**: Autorefresh is now ON by default (120s). Added `--autorefresh-off` to disable it (single fetch); `--autorefresh [SECONDS]` now only overrides the refresh interval (default 120). The widget's A/M indicator now shows "A" by default and "M" with `--autorefresh-off`. Updated the `--widget` help text and README (usage examples, minidisplay/notify/widget sections, roadmap).
