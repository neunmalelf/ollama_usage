- **20260824084340**: Ctrl+Q now stops the app. The widget binds `<Control-q>`/`<Control-Q>` to `_quit()` on both the root and canvas (frameless window has no close button); the GUI binds them on its root. Added widget and GUI tests asserting the bindings. Also aligned the plan-name tests/docs to the (uppercase) display behavior of `plan_display_name`. Bumped version to 1.0.20260824084340.
