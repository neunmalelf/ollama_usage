- **20260912083439**: New --dataonly option: prints one machine-readable line for calling programs and agents - subscription;percent_used_session;seconds_until_session_reset;percent_used_weekly;seconds_until_weekly_reset;web_search;web_fetch (e.g. PRO;48.4;11880;49.3;172800;34;0). Single fetch, no colors, no countdown, so agents can track the remaining token contingent and the reset times. Documented in README (new Dataonly section), man page (--dataonly entry) and tldr; help text includes the format.
