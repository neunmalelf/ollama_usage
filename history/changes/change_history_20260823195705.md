- **20260823195705**: Rolled the predefined plan-name display out to the whole codebase, not just the widget. The CLI (standard output, --minidisplay, --minidisplay-horizontal) and the GUI now use the `PLAN_NAMES` / `plan_display_name()` globals from scraper.py, so the subscription is always shown capitalized (e.g. "Pro") everywhere instead of the raw lowercase key. Updated CLI --help examples, README examples, and the CLI/GUI tests. Bumped version to 1.0.20260823195705.
