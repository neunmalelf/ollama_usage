- **20260822195931**: Replaced _compile.sh with _build as the Nuitka build script for ollama-usage (adapted the existing _build, which was for a different project, to build ollama-usage: package/entry point/output name, Tcl/Tk handling, icon, version metadata, copy to Scripts dir, and the --release option). Removed _compile.sh. Fixed the _build script's __VERSION__ from the YYYYMMDDhhmmss timestamp format to a proper 1.0.0.
