- **20260823194531**: The widget's top-right status indicator is now conditional: it shows "A" when the widget is launched with --autorefresh and "M" otherwise. Added an `autorefresh` parameter to `OllamaWidget.__init__` and `launch_widget`; the CLI passes `args.autorefresh is not None`. Both compact and full views draw the letter via a new `_indicator_letter()` helper, keeping the green/red color logic unchanged. Updated the --widget help text and README, and added widget tests for the A/M cases. Bumped version to 1.0.20260823194531.
