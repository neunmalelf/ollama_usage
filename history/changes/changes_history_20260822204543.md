- **20260822204543**: Fixed --minidisplay --autorefresh so the terminal is cleared at the start of each refresh loop (before the first display), not only after the loop exits. Previously the clear was skipped for minidisplay in the autorefresh loop, so the old prompt stayed visible until the program was closed.
