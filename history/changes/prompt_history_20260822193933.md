## 20260822193933
prompt: In --minidisplay output, show the plan value right after "olu" as `olu (<plan>)` instead of at the end of the line. Define named color constants for time components (days, hours, minutes, seconds, values, labels) up front — like the existing _PLAN_COLOR — and use them across the codebase (CLI, GUI, widget), including coloring the `dd hh mm` in --minidisplay. Color spec: "houers is yellow minutes is cyan seconds is magenta values is cyan labels is white".

Also: (a) when closed the gui and widget should remember its position on the screen and its size; (b) when using --widget --theme minimal and the widget is closed it shows "while executing \"2052755886208_quit\" (menu invoke)" and then the program hangs for some seconds.

Also: in the help show the line "Shows a timestamp footer with the next refresh time." on the next line under "Refresh continuously ...".
