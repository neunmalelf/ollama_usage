- **20260822195044**: Fixed widget position/size persistence. The --position CLI default was "top-left", which made the widget always use the named corner and never restore the saved position. Changed the default to None so the widget restores the last saved position (and size) on start unless --position/--size are given explicitly. Added tests/test_widget.py covering size resolution, state roundtrip, and position restore/save.
