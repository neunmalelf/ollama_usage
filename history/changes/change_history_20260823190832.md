- **20260823190832**: Added support for the new "web fetch" usage stat on ollama.com/settings. scraper.py now parses `web_fetch_requests` from `data-model="web fetch"` meter segments (mirroring web search) and exposes it in the UsageData dict/JSON. The CLI, GUI, and widget display a WebFetch line (omitted when absent), and the compact minidisplay/widget line now shows `ws:` for web search (was `wr:`) and `wr:` for web fetch. Updated scraper/cli/gui/widget tests, README, and CHANGELOG. Bumped version to 1.0.20260823190832.
