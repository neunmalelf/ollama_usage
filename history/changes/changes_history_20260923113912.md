- **20260923113912**: Release 3.0.20260923113912 — the widget compact line's credit balance after the fetch count (`| c: $ <balance>`), the expanded `--widget` help text (A = autorefresh by default, M = manual refresh with `--autorefresh-off`), and the `_build` `BUILD_JOBS` knob (default 1 worker, capped at half the logical CPUs). CHANGELOG `[Unreleased]` promoted to `[3.0.20260923113912] - 2026-09-23`, `__version__` bumped, standalone binary rebuilt and published; `skills/_update_documention/skill.md` committed as `6177681` (this change was requested as commit + release + push).
