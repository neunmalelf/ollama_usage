- **20260822220748**: Added a new --minidisplay-horizontal flag that shows the same info as --minidisplay but with each item on its own line (olu (plan) / s: ... / w: ... / wr: ...). It clears the terminal before showing the output (TTY only) and supports --autorefresh, redrawing the block with a countdown below it. Documented in README and --help; added tests.
