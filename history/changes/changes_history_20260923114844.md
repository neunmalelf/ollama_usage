- **20260923114844**: The program name is `ollama_usage` everywhere (was `ollama-usage`): `--version` prints `ollama_usage <version>`, `pyproject.toml` distribution name and console script are `ollama_usage` (PEP 503 normalizes `_`/`-`, so installed distributions and `pip install ollama_usage[notify]` hints keep resolving), `uv.lock` root entry renamed, `argparse` now pins `prog="ollama_usage"` so `--help` shows the same usage line from the binary, `python -m ollama_usage` and the tldr page, GUI `APP_NAME` and the widget window title use the underscore form, the man page is `.TH OLLAMA_USAGE` and the tldr page moved to `tldr/ollama_usage.md` (installed as `ollama_usage.page.md` via `_build`'s renamed `CONFIGURATION_TLDR_PAGE`/`CONFIGURATION_TLDR_NAME`), and README/man/tldr examples all call `ollama_usage`. Deliberately unchanged: the legacy dotted settings filenames `~/.ollama-usage-gui.cfg` / `~/.ollama-usage-widget.cfg` (migration source for older releases) and the timestamped `history/` records.
