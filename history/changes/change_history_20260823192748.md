- **20260823192748**: CLI autorefresh now returns after printing "Stopped." on Ctrl+C so the message is no longer overwritten by a final re-display. Widget layout fixes: the compact widget is wider (320x30 → 400x30) with more padding so the minidisplay line has room before the "A" indicator; the full widget is taller (240x172 → 240x186) so the "Web fetch requests:" line is no longer cut off at the bottom. Added predefined `PLAN_NAMES` globals and a `plan_display_name()` helper in scraper.py; the widget now always shows the canonical subscription name (e.g. "Pro") in both compact and full modes. Updated widget/scraper tests. Bumped version to 1.0.20260823192748.
