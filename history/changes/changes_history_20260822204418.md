- **20260822204418**: --minidisplay now clears the terminal (ANSI clear-screen + cursor home) before printing the single line, so only the information is visible and not the previous prompt. The clear is emitted only when stdout is a TTY, so piped output is unaffected.
