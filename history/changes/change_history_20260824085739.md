- **20260824085739**: Centralized the keyboard shortcuts. New `ollama_usage/shortcuts.py` defines the quit keys (`QUIT_KEYS`) once and provides a single `bind_quit(target, callback)` helper. The widget and GUI now both import and call `bind_quit` instead of repeating the key strings inline. Added `tests/test_shortcuts.py`. Bumped version to 1.0.20260824085739.
